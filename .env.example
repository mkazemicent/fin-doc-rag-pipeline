# Infrastructure
OLLAMA_BASE_URL=http://localhost:11434
CHROMA_HOST=localhost
CHROMA_PORT=8000

# Models
LLM_MODEL=qwen2.5:14b
EMBEDDING_MODEL=nomic-embed-text
NUM_CTX=8192
NUM_GPU=35

# Data root (set via docker-compose for containerized runs)
# DATA_DIR=/app/data
